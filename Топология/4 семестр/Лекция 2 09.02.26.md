[[Топология 4 семестр - HOME]]
# Операции на множествах.

**Определение**: Множество $\Omega$ называется *окрестностью* точки $x$ топологического пространства $(X,\tau)$, если $\exists u\in\tau$, $u \owns x$.

**Определение**: Всякое открытое множество $u\in\tau, u\owns x$, является *открытой окрестностью* точки $x$.

Открытую окрестность далее будем называть просто окрестностью.
![[Syncing/Топология/4 семестр/медиа/Лекция 2 рис 1.png]]

**Теорема 1**: Множество открыто в $(X,\tau)$ $\iff$ каждая точка входит вместе с некоторой окрестностью.

Доказательство:  
$\Rightarrow$: Предположим, что $U$ есть открытое множество. Тогда $\forall x\in U$ имеем $U$ — окрестность точки $x$: $U=u(x)$.  
Следовательно, $u(x)\subset U$ ⇒ $u(x)\subset U$.

$\Leftarrow$: Пусть $\forall x\in U$ существует окрестность $u_x\in\tau$ такая, что $x\in u_x\subset U$.  
Тогда  
$$
U=\bigcup_{x\in U}u_x,
$$  
и поскольку каждое $u_x\in\tau$, то по аксиоме 2 топологии $U\in\tau$.  
Следовательно, $U$ открыто.